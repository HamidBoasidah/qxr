# قائمة مرجعية للأمان قبل النشر في الإنتاج

## 🔧 إعدادات .env للإنتاج

```env
# البيئة
APP_ENV=production
APP_DEBUG=false
APP_URL=https://yourdomain.com

# قاعدة البيانات
DB_PASSWORD=strong_random_password_here

# الجلسات
SESSION_ENCRYPT=true
SESSION_SECURE_COOKIE=true
SESSION_LIFETIME=60  # تقليل مدة الجلسة

# الأمان
SANCTUM_STATEFUL_DOMAINS=yourdomain.com
```

## 🛡️ إعدادات الأمان المطلوبة

### 1. إضافة Security Headers Middleware
```bash
php artisan make:middleware SecurityHeaders
```

### 2. تحسين File Upload
- إضافة فحص MIME type الحقيقي
- إضافة virus scanning إذا أمكن
- تحديد مجلد منفصل للملفات المرفوعة

### 3. إعداد Rate Limiting شامل
- حماية جميع routes الحساسة
- إعداد rate limiting مختلف لكل نوع عملية

### 4. إعداد Logging
- تسجيل محاولات الدخول الفاشلة
- تسجيل العمليات الحساسة
- إعداد تنبيهات للأنشطة المشبوهة

### 5. إعداد Backup
- نسخ احتياطية منتظمة لقاعدة البيانات
- نسخ احتياطية للملفات المرفوعة

## 🔍 اختبارات الأمان المطلوبة

- [ ] اختبار SQL Injection
- [ ] اختبار XSS
- [ ] اختبار File Upload vulnerabilities
- [ ] اختبار CSRF protection
- [ ] اختبار Rate Limiting
- [ ] اختبار Session Security

## 📋 مراجعة نهائية

- [ ] تغيير جميع كلمات المرور الافتراضية
- [ ] إزالة أي بيانات اختبار
- [ ] تفعيل HTTPS
- [ ] إعداد Firewall
- [ ] تحديث جميع المكتبات
- [ ] إعداد monitoring