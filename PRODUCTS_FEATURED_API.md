# Products Featured APIs - المنتجات المميزة

تم إضافة API endpoints جديدة للمنتجات المميزة في التطبيق.

## 📋 المحتويات

1. [المنتجات الحديثة (وصل حديثاً)](#المنتجات-الحديثة-وصل-حديثاً)
2. [المنتجات الأكثر مبيعاً](#المنتجات-الأكثر-مبيعاً)
3. [كيفية الاختبار](#كيفية-الاختبار)

---

## 1️⃣ المنتجات الحديثة (وصل حديثاً)

### Endpoint
```
GET /api/products/recent
```

### الوصف
يرجع المنتجات النشطة مرتبة حسب تاريخ الإضافة من الأحدث إلى الأقدم.

### Headers
```
Authorization: Bearer {token}
Accept: application/json
```

### Query Parameters

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `per_page` | integer | No | 10 | عدد المنتجات في الصفحة |
| `category_id` | integer | No | - | فلترة حسب الفئة |
| `search` | string | No | - | البحث في الاسم، الوصف، أو SKU |

### مثال على الطلب

```bash
curl -X GET "http://localhost:8000/api/products/recent?per_page=10" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Accept: application/json"
```

### مثال على الاستجابة

```json
{
  "success": true,
  "message": "تم جلب المنتجات الحديثة بنجاح",
  "status_code": 200,
  "data": {
    "current_page": 1,
    "data": [
      {
        "id": 15,
        "name": "منتج جديد",
        "sku": "PRD-001",
        "description": "وصف المنتج",
        "price": 150.00,
        "main_image": "products/image.jpg",
        "is_active": true,
        "category": {
          "id": 1,
          "name": "إلكترونيات"
        },
        "company": {
          "id": 5,
          "name": "شركة النجاح"
        },
        "images": [],
        "active_offers": []
      }
    ],
    "per_page": 10,
    "total": 25,
    "last_page": 3
  }
}
```

### الميزات
- ✅ يرجع المنتجات **النشطة فقط** (`is_active = true`)
- ✅ مرتبة حسب **تاريخ الإنشاء** من الأحدث للأقدم
- ✅ يدعم **الترقيم** (Pagination)
- ✅ يدعم **الفلترة** حسب الفئة
- ✅ يدعم **البحث** في الاسم والوصف والـ SKU
- ✅ يتضمن بيانات الشركة والفئة والصور

---

## 2️⃣ المنتجات الأكثر مبيعاً

### Endpoint
```
GET /api/products/best-selling
```

### الوصف
يرجع المنتجات النشطة التي لديها مبيعات، مرتبة حسب إجمالي الكمية المباعة من الأعلى للأقل.

### Headers
```
Authorization: Bearer {token}
Accept: application/json
```

### Query Parameters

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `per_page` | integer | No | 10 | عدد المنتجات في الصفحة |
| `category_id` | integer | No | - | فلترة حسب الفئة |
| `search` | string | No | - | البحث في الاسم، الوصف، أو SKU |

### مثال على الطلب

```bash
curl -X GET "http://localhost:8000/api/products/best-selling?per_page=10" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Accept: application/json"
```

### مثال على الاستجابة

```json
{
  "success": true,
  "message": "تم جلب المنتجات الأكثر مبيعاً بنجاح",
  "status_code": 200,
  "data": {
    "current_page": 1,
    "data": [
      {
        "id": 8,
        "name": "منتج مشهور",
        "sku": "PRD-008",
        "description": "منتج الأكثر طلباً",
        "price": 200.00,
        "main_image": "products/popular.jpg",
        "is_active": true,
        "total_sold": 150,
        "purchase_count": 45,
        "category": {
          "id": 1,
          "name": "إلكترونيات"
        },
        "company": {
          "id": 5,
          "name": "شركة النجاح"
        },
        "images": [],
        "active_offers": []
      },
      {
        "id": 12,
        "name": "منتج رائج",
        "price": 180.00,
        "total_sold": 120,
        "purchase_count": 38,
        ...
      }
    ],
    "per_page": 10,
    "total": 15,
    "last_page": 2
  }
}
```

### الميزات
- ✅ يرجع المنتجات **النشطة فقط** (`is_active = true`)
- ✅ يرجع المنتجات **التي لديها مبيعات فقط** (`total_sold > 0`)
- ✅ مرتبة حسب **إجمالي الكمية المباعة** من الأعلى للأقل
- ✅ يتضمن حقل `total_sold` (إجمالي الكمية المباعة) في كل منتج
- ✅ يتضمن حقل `purchase_count` (عدد مرات الشراء) في كل منتج
- ✅ يدعم **الترقيم** (Pagination)
- ✅ يدعم **الفلترة** حسب الفئة
- ✅ يدعم **البحث** في الاسم والوصف والـ SKU

### الحقول الإضافية

#### `total_sold`
إجمالي الكمية المباعة من المنتج عبر جميع الطلبات (مجموع qty من order_items).

#### `purchase_count`
عدد مرات الشراء للمنتج (عدد السجلات في order_items). يمثل عدد المرات التي تم فيها شراء المنتج.

---

## 🧪 كيفية الاختبار

### 1. استيراد ملف Postman

1. افتح Postman
2. اذهب إلى **File** > **Import**
3. اختر الملف: `postman/Products_Featured_API.postman_collection.json`
4. سيتم استيراد Collection بها طلبين:
   - Recent Products - وصل حديثاً
   - Best Selling Products - الأكثر مبيعاً

### 2. إعداد المتغيرات (Variables)

قبل الاختبار، قم بتعيين المتغيرات التالية في Postman:

| Variable | Value | Description |
|----------|-------|-------------|
| `base_url` | `http://localhost:8000/api` | رابط الـ API |
| `token` | `YOUR_AUTH_TOKEN` | الـ Bearer token للمصادقة |

**للحصول على الـ token:**
1. استخدم endpoint تسجيل الدخول: `POST /api/login`
2. انسخ الـ `token` من الاستجابة
3. ضعه في متغير `token` في Postman

### 3. تشغيل الاختبارات

1. اختر أحد الطلبات من Collection
2. اضغط على **Send**
3. ستظهر نتائج الاختبارات التلقائية في تبويب **Test Results**

### 4. الاختبارات التلقائية

كل طلب يحتوي على اختبارات تلقائية تتحقق من:
- ✅ نجاح الطلب (Status 200)
- ✅ وجود الحقول الأساسية في الاستجابة
- ✅ صحة بنية البيانات
- ✅ صحة بنية Pagination
- ✅ صحة بيانات المنتج
- ✅ (للأكثر مبيعاً) التحقق من الترتيب الصحيح

---

## 📝 ملاحظات إضافية

### الفلاتر المدعومة

يمكنك دمج عدة فلاتر في طلب واحد:

```bash
GET /api/products/recent?per_page=20&category_id=5&search=laptop
```

### الترقيم (Pagination)

للانتقال بين الصفحات:
```bash
GET /api/products/recent?page=2&per_page=10
```

### الأداء

- الـ endpoints مُحسّنة للأداء
- استخدام `withCount()` لحساب المبيعات بكفاءة
- استخدام `latest()` للترتيب السريع

---

## 🔧 التغييرات التقنية

### ملفات تم تعديلها

1. **`app/Http/Controllers/Api/ProductController.php`**
   - إضافة method `recent()`
   - إضافة method `bestSelling()`

2. **`app/Models/Product.php`**
   - إضافة relationship `orderItems()`

3. **`routes/api.php`**
   - إضافة route: `GET /products/recent`
   - إضافة route: `GET /products/best-selling`

4. **`postman/Products_Featured_API.postman_collection.json`**
   - ملف Postman جديد للاختبار

---

## 🎯 حالات الاستخدام في التطبيق

### 1. قسم "وصل حديثاً"
```javascript
// استدعاء API للمنتجات الحديثة
fetch('http://localhost:8000/api/products/recent?per_page=10', {
  headers: {
    'Authorization': `Bearer ${token}`,
    'Accept': 'application/json'
  }
})
```

### 2. قسم "الأكثر مبيعاً"
```javascript
// استدعاء API للأكثر مبيعاً
fetch('http://localhost:8000/api/products/best-selling?per_page=10', {
  headers: {
    'Authorization': `Bearer ${token}`,
    'Accept': 'application/json'
  }
})
```

### 3. عرض المنتجات مع العروض
كلا الـ endpoints يدعمان جلب العروض النشطة مع المنتجات من خلال العلاقة `activeOffers`.

---

## ✅ جاهز للاستخدام!

الآن يمكنك:
1. ✅ استخدام الـ APIs في التطبيق
2. ✅ اختبارها عبر Postman
3. ✅ دمجها في واجهة المستخدم
4. ✅ عرض المنتجات الحديثة والأكثر مبيعاً

---

**تاريخ الإنشاء:** 24 فبراير 2026  
**الإصدار:** 1.0.0
